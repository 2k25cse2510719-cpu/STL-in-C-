// #include<iostream>
// #include<array>//important to include if you want to use array as a container .
// using namespace std ;
// int main (){
//     std::array<int,5> arr;//"arr" is a data member , because it contains data .
//      cout<<"Enter Elements"<<endl;
//  for(int i =0;i<arr.size()//can use arr.size() here because std::array is a container// 
//  ;i++){//this is a data function because it performs function on data member//
//  cin>>arr[i];
//  }
//  cout<<arr.front()<<endl;//prints the first value in the array
// cout<<arr.back()<<endl;//prints the last value in the array
// std::array<int ,5>brr={1,2,3,4,5};
// for(int i=0;i<arr.size();i++){
//     cout<<arr[i]<<" "<<endl;
// }
// brr.swap(arr);//used to swap two whole arrays (could not have done it in an ordinary c array
// // // ,std::arr is a special c++ array , is very compatible with stl , algorithms & member functions )
// // //both the arrays need to be of the same size
//    cout<<arr[i]<<" ";
//  }
//  cout<<brr.at(3);
//  int *p=brr.data();//return then stores (pointer) address of the first element in p.
//  cout<<p;
// arr[4]=arr.operator[](4);
// }
// also we can initialize array elements later in this 
// for example :-
// st::array<int,5>myarray;
// myarray={1,2,3,4,5,};
// This was not possible in classic c type array.
